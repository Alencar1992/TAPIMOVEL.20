name: P11 aplicar padronização do núcleo

on:
  push:
    branches:
      - agent/p11-core-utils
    paths:
      - scripts/apply-p11.js
      - .github/workflows/p11-aplicar.yml

permissions:
  contents: write

jobs:
  aplicar:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: agent/p11-core-utils
          fetch-depth: 0

      - name: Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Aplicar P11
        run: node scripts/apply-p11.js

      - name: Validar
        run: npm run ci

      - name: Registrar alterações
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps-script scripts tests
          if git diff --cached --quiet; then
            echo "Nenhuma alteração adicional para registrar."
            exit 0
          fi
          git commit -m "P11: aplicar padronização do núcleo"
          git push origin HEAD:agent/p11-core-utils
